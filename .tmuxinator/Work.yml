# ~/.tmuxinator/Work.yml

name: Work
root: ~/shared/server
windows:
  - editor:
      layout: main-horizontal
      panes:
        - editor:
          - source ../dev2/bin/activate

        - ansible:
          - source ../server2/bin/activate
